version: "3.9"
services:
  api:
    container_name: 1CServices.api
    environment:
      - API_PORT=${API_PORT}
      - DB_DSN=${DB_DSN}
    image: donskova1ex/api:latest
    ports:
      - 8080:${API_PORT}